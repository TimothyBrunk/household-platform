package com.household.todoservice.service;

import com.household.todoservice.dto.UserResponse;
import org.mapstruct.Mapper;

/**
 * Mapper interface for User-related conversions.
 * 
 * Note: In a real implementation, this would integrate with an external user service.
 */
@Mapper(componentModel = "spring")
public interface UserMapper {

    // This mapper would typically integrate with an external user service
    // For now, it's a placeholder for future user service integration
}
